# reshape函数
注：reshape函数需要有一个共同前缀，该函数主要把变量拆解成：xx + j

其中xx是共同前缀，j为特殊后缀

i是在转置过程中不变的量

假如你 reshape 之前的变量长这样：

tem      aa广东省    aa浙江省    aa江苏省

东部        0           1           1

中部        0           0           0

西部        0           0           0

Stata看到：

aa广东省

aa浙江省

aa江苏省

共同前缀是：aa

于是：reshape long aa, i(tem) j(省份) s

就是把：aa广东省  , 拆成：aa + 广东省

其中：aa变成数值变量，而：广东省  进入新的：省份变量。

最后那个：s是 string 的缩写。

# stata的for循环

Foreach i of varlist _all指的是对所有变量进行遍历，i作为临时变量放在里面

# merge函数

Stata 里当前已经打开的表叫 **master data（主表）**，using 后面的文件叫 **using data（被合并表）**。

先看最简单的 1:1。

假设你现在内存里的数据是“学生成绩表”：

|   |   |   |
|---|---|---|
|**学号**|**姓名**|**分数**|
|001|张三|85|
|002|李四|90|
|003|王五|78|

另一个文件 学生信息.dta 是：

|   |   |   |
|---|---|---|
|**学号**|**性别**|**班级**|
|001|男|投资241|
|002|女|投资241|
|003|男|投资242|

你想得到：

|   |   |   |   |   |
|---|---|---|---|---|
|**学号**|**姓名**|**分数**|**性别**|**班级**|
|001|张三|85|男|投资241|
|002|李四|90|女|投资241|
|003|王五|78|男|投资242|

代码就是：

merge 1:1 学号 using "学生信息.dta"

这里的意思是：

merge   1:1   学号   using "学生信息.dta"

        ↑      ↑

      对应关系  按谁匹配

1:1 的意思是：

主表里面，一个学号只能出现一次；  
using 表里面，一个学号也只能出现一次。

再看 m:1:

假设主表现在是：

|   |   |   |
|---|---|---|
|**省份**|**年份**|**GDP**|
|广东省|2022|129000|
|广东省|2023|135700|
|广东省|2024|141600|
|浙江省|2022|77700|
|浙江省|2023|82500|
|浙江省|2024|90100|

注意：广东省出现了3次;浙江省出现了3次

因为每个省有很多年，所以主表里“省份”不是唯一的。

现在你有一个 东中西划分.dta：

|   |   |
|---|---|
|**省份**|**东中西划分**|
|广东省|东部|
|浙江省|东部|
|四川省|西部|

这里一个省只出现一次。

你希望把“东部、中部、西部”加到每一年上：

|   |   |   |   |
|---|---|---|---|
|**省份**|**年份**|**GDP**|**东中西划分**|
|广东省|2022|129000|东部|
|广东省|2023|135700|东部|
|广东省|2024|141600|东部|
|浙江省|2022|77700|东部|
|浙江省|2023|82500|东部|
|浙江省|2024|90100|东部|

代码：

merge m:1 省份 using "东中西划分.dta"

这里：

m : 1

↑   ↑

主表 using表

m 就是 many，很多条。

因此：

主表                          using表

广东 2022 ─┐

广东 2023 ─┼──────→ 广东 东部

广东 2024 ─┘

浙江 2022 ─┐

浙江 2023 ─┼──────→ 浙江 东部

浙江 2024 ─┘

1:m 则正好倒过来。

假设当前主表：

|   |   |
|---|---|
|**学号**|**姓名**|
|001|张三|
|002|李四|

也就是说每个学生只有一行。

using 文件 考试成绩.dta：

|   |   |   |
|---|---|---|
|**学号**|**考试**|**分数**|
|001|期中|80|
|001|期末|90|
|002|期中|75|
|002|期末|88|

using 表里面一个学号出现很多次。

所以：merge 1:m 学号 using "考试成绩.dta"

结果：

|   |   |   |   |
|---|---|---|---|
|**学号**|**姓名**|**考试**|**分数**|
|001|张三|期中|80|
|001|张三|期末|90|
|002|李四|期中|75|
|002|李四|期末|88|

# 4种取整函数

floor(3.7)  = 3      向下取整

ceil(3.2)   = 4      向上取整

round(3.6)  = 4      四舍五入

int(3.7)    = 3      去掉小数部分/向0截断

# order函数

order varlist [, options]

其中 varlist 就是“你想移动的变量”。

最常用的情况有下面这些：默认情况下，不写任何选项，就是把指定变量移到最前面：

order year

原来：price profit year province

变成：year price profit province

如果一次写多个：order province year

就会按照你写的顺序放在最前：province year price profit

所以你也可以把它理解成：order A B C

就是：把 A、B、C 按这个顺序挪到最前面。

如果想把变量放到最后，用：order profit, last

例如原来：province year profit price

变成：province year price profit

before() 是把变量放在某个变量前面。

比如：order year, before(price)

原来：province price profit year

变成：province year price profit

也就是：把 year 搬到 price 前面。

相反，after() 是搬到某个变量后面：order year, after(province)

原来：price year province profit

变成：price province year profit

# sort函数
比如现在有这张数据：

|姓名|年龄|分数|
|---|---|---|
|张三|20|85|
|李四|18|90|
|王五|19|78|

如果执行：

```
sort 年龄
```

Stata 会按照“年龄从小到大”排列：

|姓名|年龄|分数|
|---|---|---|
|李四|18|90|
|王五|19|78|
|张三|20|85|

所以 `sort` 默认就是“升序”。

你截图里的语法：

```
sort varlist
```

这里 `varlist` 可以有一个变量，也可以有多个变量。

例如：

```
sort 省份 年份
```

它不是“同时乱排”，而是有优先级：

先按 `省份` 排；如果省份相同，再按 `年份` 排。

假设原始数据是：

|省份|年份|GDP|
|---|---|---|
|浙江|2022|80000|
|广东|2023|135000|
|广东|2021|124000|
|浙江|2021|75000|
|广东|2022|129000|

执行：

```
sort 省份 年份
```

会变成：

|省份|年份|GDP|
|---|---|---|
|广东|2021|124000|
|广东|2022|129000|
|广东|2023|135000|
|浙江|2021|75000|
|浙江|2022|80000|

你可以把：

```
sort 省份 年份
```

理解成：

> 第一级排序变量是省份；  
> 省份相同的时候，再看年份。

这和 Excel 里的“主要关键字 + 次要关键字”是一个逻辑。

如果有三个变量，比如：

```
sort 省份 年份 企业
```

那就是：

```
先按省份
↓
同省份内按年份
↓
同省同年内再按企业
```

另外，`sort` 默认只能升序。
如果要用降序，要用 gsort


# encode函数

`encode` 可以理解成：

> 把“字符串类别”转换成“带文字标签的数值类别”。

它最适合处理这种变量：

```
省份
广东省
广东省
浙江省
四川省
浙江省
```

这些值本质上是字符串，Stata 不能直接把它们当作面板 ID 或很多统计模型中的分类编号使用。这时候可以：

```
encode 省份, gen(省份代码)
```

执行后，数据实际上会变成类似这样：

| 省份  | 省份代码实际存储值 | Stata显示 |
| --- | --------- | ------- |
| 广东省 | 1         | 广东省     |
| 广东省 | 1         | 广东省     |
| 四川省 | 2         | 四川省     |
| 浙江省 | 3         | 浙江省     |


# label函数
`label` 可以理解成：给变量或者数值“贴一个更容易看懂的说明标签”。

你截图里的：

```
label var unemployment "近似城镇登记失业率(%)"
```

意思是：

> 变量真正的名字仍然叫 `unemployment`，只是给它附加一个中文说明：“近似城镇登记失业率(%)”。

所以它和 `rename` 完全不一样。

假设原来有：

```
gen unemployment = 5.2
```

变量名是：

```
unemployment
```

然后执行：

```
label var unemployment "近似城镇登记失业率(%)"
```

以后 Stata 内部仍然认：

```
unemployment
```

比如回归还是：

```
reg GDP unemployment
```

不能写：

```
reg GDP 近似城镇登记失业率(%)
```

因为后面这个只是“标签”，不是真正的变量名。


# vce函数

```
vce(cluster city)
```

最核心的意思是：

> 允许“同一个城市内部不同年份的误差项彼此相关”。

比如你的面板数据是：

```
city      年份     unemployment
广州      2015       ...
广州      2016       ...
广州      2017       ...
深圳      2015       ...
深圳      2016       ...
深圳      2017       ...
```

普通 OLS 在计算标准误时，比较强地依赖不同观测之间误差独立这一类假设。但面板数据里，同一个城市今年和明年的情况往往不是完全独立的。

例如广州：

广州2019年的某些未观测因素
            ↓
广州2020年可能还存在
            ↓
广州2021年也可能继续存在

因此：

vce(cluster city)

就是告诉 Stata：

广州内部的误差可以相关，深圳内部也可以相关；但是把不同城市当作不同 cluster 来处理。

这里一个 city 就是一个 cluster。

# tab函数
tab函数就是在做频数分布表的意思

# est函数

这里的：

```
est store east
```

完整写法是：

```
estimates store east
```

意思就是：

> 把刚刚这一次回归结果保存起来，并命名为 `east`。

# 异质性检验

```
c.higheredu
```

里的 `c.` 表示 Stata 把 `higheredu` 当作连续变量。

constant.higheredu
```
ib2.region
```

里的：

```
i  = 分类变量
b2 = 把第2组设为基准组
```

你前面编码是：

```
2 = 东部
3 = 中部
4 = 西部
```

所以：

```
ib2.region
```

就是明确告诉 Stata：

> 以东部地区 `region==2` 为参照组。

然后：

```
ib2.region#c.higheredu
```

就是构造：

```
地区 × higheredu
```

的交互项。


所谓异质性，本质上问的是：

同一个解释变量 \(X\)，对不同类型的样本，影响是不是一样？

比如你的研究是：

高等教育水平 higheredu 会不会影响城镇失业率 unemployment？

基准回归默认认为，全国所有城市的影响都一样：

$$ unemployment_{it} =\alpha+\beta higheredu_{it}+\cdots+\varepsilon_{it} $$

假设最后估出来：

$$ \beta=-0.2 $$

意思是，在其他条件不变时，higheredu 每增加 1 个单位，失业率平均下降 0.2 个单位。

但问题来了：东部、中部、西部经济结构并不完全一样，那么 -0.2 这个影响会不会其实是几种不同影响混在一起之后得到的平均值？

假设真实情况是：

$$ \begin{aligned} 东部：&\quad \beta_E=-0.4\\ 中部：&\quad \beta_C=-0.1\\ 西部：&\quad \beta_W=+0.2 \end{aligned} $$

那就说明 higheredu 对失业率的影响存在“地区异质性”。

因为：
```
东部：higheredu ↑1 → unemployment ↓0.4

中部：higheredu ↑1 → unemployment ↓0.1

西部：higheredu ↑1 → unemployment ↑0.2
```
同一个 `higheredu`，到了不同地区，影响不一样。
这就是异质性最直白的意思。

第一种办法：分组回归

最容易想到的方法就是把三个地区拆开跑。

* 东部
reg unemployment higheredu fdi lnpgdp hospital ///
    i.city i.年份 if region==2, vce(cluster city)

* 中部
reg unemployment higheredu fdi lnpgdp hospital ///
    i.city i.年份 if region==3, vce(cluster city)

* 西部
reg unemployment higheredu fdi lnpgdp hospital ///
    i.city i.年份 if region==4, vce(cluster city)

假设你真的得到：

地区	higheredu系数	p值
东部	-0.40	0.001
中部	-0.10	0.300
西部	0.20	0.150

你可以初步看出来：

higheredu 的估计系数在东、中、西部存在差别。

但这里有个非常重要的问题：

“东部显著、中部不显著” ≠ “东部和中部的系数显著不同”。

这是异质性分析中特别容易犯的错误。

为什么？

假设：

$$ \hat\beta_E=-0.40,\quad p=0.04 $$

而：

$$ \hat\beta_C=-0.35,\quad p=0.08 $$

一个显著，一个不显著。

但：

$$ -0.40-(-0.35)=-0.05 $$

两者其实非常接近。

所以仅仅看三个分组回归，能看到“系数好像不一样”，但还没有正式回答：

它们之间的差异是否具有统计显著性？

于是就要用你现在看到的“交互项异质性检验”。

第二种办法：交互项检验

假设：

region = 2 → 东部
region = 3 → 中部
region = 4 → 西部

我们把东部作为基准组。

你的代码：

reg unemployment ///
    c.higheredu ///
    ib2.region#c.higheredu ///
    fdi lnpgdp hospital ///
    i.city i.年份 ///
    if inlist(region,2,3,4), ///
    vce(cluster city)

先忽略其他控制变量，核心模型其实就是：

$$ Y = \alpha +\beta X +\gamma_C(D_C\times X) +\gamma_W(D_W\times X) +\varepsilon $$

这里：

$$ X=higheredu $$

而：

$$ D_C= \begin{cases} 1,&中部\\ 0,&其他 \end{cases} $$ $$ D_W= \begin{cases} 1,&西部\\ 0,&其他 \end{cases} $$

东部没有单独的交互虚拟变量，因为东部被设成了基准组。

假设 Stata 最后给你：

higheredu                     -0.40
3.region#c.higheredu           0.30
4.region#c.higheredu           0.60

这三个数千万不要直接理解成：

东部 = -0.40
中部 =  0.30
西部 =  0.60

不是这样的。

正确理解是：

higheredu = -0.40
→ 东部自己的效应

3.region#c.higheredu = +0.30
→ 中部比东部高0.30

4.region#c.higheredu = +0.60
→ 西部比东部高0.60

于是东部：

$$ \beta_E=-0.40 $$

中部：

$$ \beta_C=-0.40+0.30=-0.10 $$

西部：

$$ \beta_W=-0.40+0.60=0.20 $$

正好就是：

地区	higheredu 的实际效应
东部	-0.40
中部	-0.10
西部	+0.20

现在真正的“异质性检验”就出现了。

假设 Stata 输出：

higheredu                     -0.40***     p=0.001
3.region#c.higheredu           0.30**      p=0.020
4.region#c.higheredu           0.60***     p=0.001

那么：

3.region#c.higheredu = 0.30，p=0.020

检验的实际上是：

$$ H_0:\beta_C-\beta_E=0 $$

也就是：

中部和东部的 higheredu 系数没有差别。

现在 \(p=0.020<0.05\)，拒绝原假设，所以可以认为：

中部与东部之间的 higheredu 系数存在统计显著差异。

同理：

4.region#c.higheredu = 0.60，p=0.001

检验：

$$ H_0:\beta_W-\beta_E=0 $$

结果显著，所以：

西部与东部之间也存在显著差异。

这才是“正式检验异质性”。