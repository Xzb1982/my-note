几何向量角度，有序数表角度

从几何向量角度来说，决定一个向量的是其长度和它所指的方向

从有序数表的角度，向量就是一个数表:
![[Pasted image 20250217221939.png]]
每一行代表一个维度，有三行就是三维
行与行之间的数字排列不能颠倒

向量加法和向量数乘贯穿线性代数的始终

![[Pasted image 20250217222316.png]]

我们先从向量的几何意义引入，坐标轴xy，但是向量的起点必须是原点为起点
![[Pasted image 20250217222539.png]]
现在，我们引入向量的有序数表角度：
$$\begin{bmatrix} 
   a  \\  
   b   
\end{bmatrix}
$$
其中，第一行表示x坐标，第二行表示y坐标

对于有序数表的信息，我们可以这样解读：
数字代表走的距离，正负号表示走的是正侧还是负侧，数字在哪一行代表的是在哪一个轴移动

每一个向量对应每一对数组，每一对数组对应一个向量

![[Pasted image 20250217223737.png]]
现在，我们引入向量的四则运算，
向量相加减，如图所示，是线性代数唯一允许离开原点的情形

在点在向量运动的过程中，先走v向量后走w向量和直接走 v向量+w向量 的运动效果是等效的

那么从数组的角度来看：![[Pasted image 20250217224627.png]]
$$\begin{align*}
\begin{bmatrix} 
   1  \\  
   2   
\end{bmatrix}+ \begin{bmatrix}
3 \\
-1
\end{bmatrix}= \begin{bmatrix}
1+3 \\
2-1
\end{bmatrix}= \begin{bmatrix}
4 \\
1
\end{bmatrix}
\end{align*}$$

向量的数乘，就是向量的放缩

同时，向量的放缩的前面的那个系数，就是一种标量
![[Pasted image 20250217231029.png]]
